<!-- views/partials/errors.ejs -->
<% if (locals.errors) {%>
    <ul>
      <% errors.forEach(function(error) { %>
        <li style="color:red;text-decoration: underline;"><%= error.msg %></li>
      <% }); %>
    </ul>
  <% } %>